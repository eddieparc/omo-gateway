# U07 resumed by lead

Prior child stopped on provider quota before a behavior result. Existing tests were discovered in U07-list.log with exact names; no U07 production fix is present. Lead exclusively owns skills.rs, storage/db.rs and memory/store.rs until this unit is proven.

Registered before production changes: U07-commands.json contains exact commands. First test runs isolated HOME/WRITE_APPROVAL child cases: custom skill root A must receive replay (not fallback B), and staged memory must already have actual guild/channel/thread/user session metadata. Second test uses task-local barrier before write claim to force two approvers to read the same pending memory; exactly one memory row and one winning receipt must result. Real SQLite/tools and local files, no Discord. Existing tests are inherited; their unformatted layout will be corrected only in scoped hunks after behavioral proof.

RED expectations: wrong skill fallback destination, absent staged-memory session metadata, duplicate memory rows. Capture actual nonzero failures before patch; compilation/lock failures are not RED.

Captured first command behavioral RED via monitor mon_37S9HJ99274YTPKZ / bash_2: exact outer test 0 passed, 1 failed; two isolated child cases each 0 passed, 1 failed. Destination result printed A=false B=true; metadata printed session=None versus actual guild6/channel7/thread9/user8. This occurred before the lead changed skills.rs/memory/store.rs or replay destination selection. Atomic second command is still pending the shared build lock; no atomicity GREEN/RED claim yet.

First minimal change stores absolute configured root and the already validated canonical destination in staged skill payload; replay validates against that stored root and rejects changed destination. MemoryStore now ensures the real session before staging. Existing legacy payloads still accept explicit approved root; path containment remains enforced. LSP on memory/store.rs and tools/skills.rs timed out waiting for fresh diagnostics (3000ms), not reported clean.

Second RED completed before transaction edit: two Some approvals and memory_rows=2, exact test 0 passed/1 failed. Monitor ended with U07_RED_EXITS 101 101. The lead then added conditional pending deletion by id/kind/payload and memory insertion within one SQLite transaction. A losing approver returns None; an error rolls the transaction back. Dummy unknown session insertion was removed; old roundtrip fixture now supplies its actual session rather than relying on fabricated metadata.

Identical GREEN monitor mon_0KBSHWEZ0F0ASD1W exited0. Original destination/metadata exact test passes outer1 and child2 with A=true/B=false and actual metadata. Atomic test passes1: one None, one Some and memory_rows=1. Adjacent pending roundtrip1, destination controls1 and U06 containment outer1 plus all11 child scenarios pass. This proves actual Tool/File/SQLite approval surface, not Discord UI. Storage LSP reported no diagnostics. Initial scoped rustfmt failed on inherited U07 lines and one new wrapped expression; only those lines were corrected with apply_patch.

Additional postpatch controls, not claimed as prepatch RED: missing-session approval must fail and roll back pending deletion with zero fabricated sessions/memories; recorded skill root replacement by symlink must be rejected before any write, preserving pending intent. Build and final scoped format remain pending. No claim of crash-atomic filesystem plus SQLite commit: the memory operation is fully transactional; skill writes retain validated destination and serialized approval consumption.

Final lead validation2026-09-06: all three changed-file LSP diagnostics report No diagnostics found. rustfmt --check for all three passes. cargo test --lib staged_memory_ passes2; cargo test --lib tools::skills::tests passes4 plus the two U07 and eleven U06 child cases; cargo build finishes successfully in14.62s. Raw output is U07-resume-final-green.log. The command then waited in git diff pager at (END), not inside tests/build; lead sent q to exit pager. Final standalone nonpaged diff check is recorded separately. Read the full attributable diff: no other U07 files changed, baseline U06 path containment preserved, original nonstaged memory path unchanged except earlier session ensure. Tests close SQLite pools and TempDirs, subprocess cases use private HOME/WRITE_APPROVAL. No production state or Discord operations.
