# Cron gateway parity audit - 2026-09-05

Status: COMPLETE (source audit; proposed regressions/QA not executed).
Rust HEAD: `9bd62b132436b67296b7895bc6bc84a67e1a37d8`; Hermes local HEAD: `146f4ed07d1c10031c86a5e7f2ac78d11b253dd9`. Working-tree source, including existing modifications, is authoritative here, not the historical backlog. Only this report was written. No cargo suite, production execution, Discord interaction, or reference-checkout modification occurred.

LSP document-symbol request failed: daemon unreachable at `/Users/indo/.omo/lsp-daemon/v0.1.0/daemon.sock`. Used ast-grep Rust impl search, textual caller search, and bounded source reads. Programming/Rust skill read. Requested `src/platform/outbound.rs` and `tests/test_ack_hook.rs` do not exist in this checkout; actual egress is `src/discord/adapter.rs`; ACK tests are in `src/cron/{ack,scheduler}.rs`. All Rust cron/ledger files and voice-cron tests were read; Hermes cron scheduling/storage/execution/provider/guard and catalog/suggestion/classifier surfaces were inspected, plus all three assigned gateway delivery files. Catalog data and independent classifier intelligence are not gateway runtime parity requirements.

## Inventory and old-backlog disposition

Paths beginning `cron/`, `gateway/`, `tools/cronjob_tools.py`, or `agent/skill_bundles.py` in the Hermes column are relative to the reference checkout. FIX means confirmed source-level defect or explicitly labeled applicable feature gap, not a failing test observed in this audit.

**Counts: 24 inventory rows: 17 FIX, 4 PRESENT, 1 INTENTIONAL, 2 NOT-APPLICABLE.** Sixteen FIX rows describe behavioral defects; C17 is an applicable feature-surface gap. Rows can contain related sub-defects sharing one implementation owner. P0 #8/#13/#14 are partial implementations with remaining defects, not wholly missing features.

| ID | Old entry / surface | Status | Current Rust evidence | Hermes evidence | Mechanism / disposition |
|---|---|---|---|---|---|
| C01 | P0 #8 durable outbound obligations | FIX | `src/ledger/service.rs:198-353`; `src/main.rs:301-365,1297`; `src/agent/omo_backend.rs:658-677`; `src/cron/scheduler.rs:1177-1201` | `gateway/delivery_ledger.py:1-37,153-289` | Ledger and boot sweep exist, but repository callers of `record_obligation` are tests only. Actual final stream and script send never record owed output. Crash loses it; backend also returns success after final dispatch error. Sweep uses PID alone (no start stamp), so reused live PID strands old obligations; no terminal-obligation pruning exists (Hermes bounds retention/rows at delivery_ledger.py:292-322). |
| C02 | P0 #13; P1 silence; P2 agent fanout/mirroring | FIX | `src/main.rs:553-604,675-705`; `src/agent/omo_backend.rs:363-368,623-677`; `src/discord/adapter.rs:1842-1912`; `src/cron/scheduler.rs:1113-1137` | `cron/scheduler.py:3795-3866` | Agent emits directly and executor returns `None`; scheduler never receives final text. Hermes goes through scheduler save/filter/deliver. Rust posts `[SILENT]`/`NO_REPLY`, synthesizes Done for tool-only output, delivers only first Hermes destination, and bypasses delivery mirroring. Native comma target becomes invalid channel string. Local agent jobs still attempt Discord dispatch to job ID (numeric IDs can be actual channels). |
| C03 | P0 #14 finite repeat; retry/recovery/idempotency | FIX | `src/cron/scheduler.rs:695-795,918-969,1019-1103` | `cron/jobs.py:1545-1570,1610-1664,1705-1731`; `cron/executions.py:156-181` | Only success increments completed. Recurring failures never exhaust finite runs. Claims neither consume one-shot budget nor advance recurring schedule before side effects; dead-owner reclaim repeats ambiguous work. Explicit trigger increments count but ignores limit; no preclaim limit check prevents further runs. |
| C04 | P1 context_from; persistent local output | FIX | `src/cron/scheduler.rs:110-176,1106-1140`; `src/main.rs:531-541,553-578`; `src/agent/omo_backend.rs:764-778` | `cron/scheduler.py:2389-2434,3801`; `cron/jobs.py:2208` | Script-only/local results have no output persistence. DB lookup uses unscoped suffix LIKE, not job/profile provenance; real origin user replaces cron job user, so predecessor may be unfindable. `%`/`_` are allowed and act as SQL wildcards; identical job IDs in profiles leak context. Fallback reads global home after profile-local miss. |
| C05 | P1 wakeAgent; P0 #13 pre-run silence | FIX | `src/main.rs:492-505,651-672`; `src/cron/scheduler.rs:1115-1127` | `cron/scheduler.py:2363-2386,2690-2697,2748-2776` | Hermes no_agent returns before gate in Rust, delivering false-gate JSON; no_agent without script silently succeeds. Empty successful precheck still invokes agent. Executor `None` can resurrect payload notification/content even when wake gate explicitly suppressed execution. |
| C06 | P1 missing skill; P2 skill bundle | FIX | `src/main.rs:502-505,708-735,745-759,801-839` | `cron/scheduler.py:2339-2352,2451-2540`; `agent/skill_bundles.py:1-38` | Promptless skill-only jobs rejected before loading any skill. Missing skills with real prompt already warn/continue, but helper coverage hides precheck. `/bundle` is rejected as absolute path rather than normalizing slash command; Hermes explicitly strips slash. |
| C07 | P1 lifecycle footgun guard | FIX | `src/cron/guard.rs:5-29`; `src/cron/store.rs:405-413`; `src/main.rs:1024-1086`; `src/dashboard.rs:1244-1316`; `src/cron/scheduler.rs:534-599` | `cron/lifecycle_guard.py:49-66,76-141` | Import scans script filename, never file body; executor runs resolved file unchecked. Dashboard register bypasses guards altogether, including ack_command. Conversely all launchctl kickstart and systemctl restart commands are blocked even for unrelated services; unrelated omon prose can match broad restart regex. |
| C08 | P1 cron injection scanner | FIX | `src/tools/cron.rs:330-345,438-452`; `src/security/scan.rs:65-140`; `src/main.rs:531-600`; `src/cron/store.rs:405-418` | `cron/scheduler.py:2543-2600,2883-2899`; `tools/cronjob_tools.py:78-103` | Create/update scanner exists only on CronTool raw prompt. Imported jobs, REST payloads, loaded skills and predecessor/script data reach backend without assembled-prompt scanning. Do not reuse strict command scanner on legitimate injected data; Hermes uses two tiers. |
| C09 | P1 CronTool operations / read-only ownership | FIX | `src/main.rs:1222`; `src/dashboard_runtime.rs:103`; `src/tools/cron.rs:177-314,353-403,460-485`; `src/cron/store.rs:439-460` | `cron/jobs.py:1333-1499`; `cron/scheduler_provider.py:85-112` | Production constructs scheduler-less tool: trigger only rewrites next_run_at, does nothing for paused jobs and consumes scheduled one-shot on next poll. Imported jobs advertise read-only but mutations succeed and next sync resurrects delete/overwrites pause/update. Create drops repeat/context_from/skills/no_agent/ack/timeout/model and ignores enabled:false; update always recomputes next time even for name-only change. |
| C10 | P2 fanout target parsing | FIX | `src/cron/store.rs:164,221-278,316-337`; `src/cron/scheduler.rs:1290-1350`; `src/main.rs:675-685` | `cron/scheduler.py:1136-1297`; `gateway/delivery.py:161-205` | `all`/bare discord use origin only, not configured home fallback. `discord:42:43` remains chat_id `42:43` and fails numeric parse. Uppercase platform ignored. Imported array deliver fails whole-document deserialization despite native array support. |
| C11 | ACK, partial fanout, delivery failure | FIX | `src/cron/scheduler.rs:1129-1152,1177-1224`; `src/cron/ack.rs:38-61`; `src/discord/adapter.rs:1927-1933,2070-2078` | `cron/scheduler.py:1368-1387,1530-1532,2011-2013`; `gateway/delivery.py:266-313` | ACK executes per destination, including error-alert sends. First good destination ACKs before later failure; first failure aborts remaining success targets. Dead-target skip returns Ok, so no delivered message can still commit producer checkpoint/mirror. PNG attachment failure is logged then success returned. ACK extension has no direct Hermes equivalent; confirmed-delivery semantics still apply. |
| C12 | P1 dead-target short circuit | FIX | `src/discord/adapter.rs:1599-1693,1819,1842-1908,1927-1933,2008-2020,2088-2120` | `gateway/dead_targets.py:1-20,58-88,97-132`; `gateway/delivery.py:127-141` | Registry exists but is memory-only, keyed only channel across bot clients. Every HTTP 403/404 is treated as whole-chat death, including missing message edit/reference. Default no TTL and skipped sends cannot self-heal. Final Stream bypasses registry entirely. Restart forgets known failures. |
| C13 | P2 provider/base_url; workdir/toolset execution | FIX | `src/main.rs:511-529,580-603,945-995`; `src/agent/omo_backend.rs:227-244`; `src/agent/omo_protocol.rs:23-45,62-80` | `cron/scheduler.py:3343-3365` | build_cron_llm_config/build_cron_tools are not called in live executor. Only Hermes model forwarded; provider/base_url and enabled_toolsets ignored. Requested workdir affects instruction file read/script cwd but not agent cwd, which is provisioned from default config. Native model ignored too. Gateway contract gap, not a request to port Hermes intelligence: implement supported OMO routing or reject unsupported overrides instead of accepting silently. |
| C14 | P2 cron mirroring; multi-bot identity | FIX | `src/cron/scheduler.rs:1180-1189,1231-1276`; `src/mirror.rs:61-121`; `src/tools/cron.rs:111-127`; `src/main.rs:308-326,553-578`; `src/models/session.rs:24-42` | `cron/scheduler.py:656-757` | Created reminder drops bot/thread delivery coordinates; all newly built delivery/recovery keys have bot_id None => default bot. Explicit source session is reused for every fanout mirror, regardless of destination; fallback can pick another bot/thread's latest session. Existing origin.user_id also makes cron jobs share backend thread/cache identity instead of cron isolation. |
| C15 | persistent schedule interpretation | FIX | `src/cron/store.rs:193-212,426-431`; `src/cron/scheduler.rs:1361-1414` | `cron/jobs.py:720-778` | Imported cron expression is evaluated against DateTime<Utc> after first completion, dropping Hermes configured timezone. Stored next_run_at initially works, then e.g. 09:00 Seoul becomes 09:00 UTC. No timezone field is consumed. |
| C16 | persistent sync isolation / rearming | FIX | `src/cron/store.rs:316-337,393-443`; `src/main.rs:1293-1295`; `src/cron/scheduler.rs:494-502` | `cron/jobs.py:1930-1935,1960-1989`; `cron/executions.py:156-181` | One malformed job/store aborts full sync and startup, not just bad job. Old once NULL-state preservation runs before detecting changed expression/time, so operator rearm remains disabled. Finite exhausted state similarly prevents increased repeat limit. Runtime last_status/last_error/last_delivery_error payload fields never updated; only cron_runs carries current state. |
| C17 | P1 CronTool runs/status | FIX (feature gap) | `src/tools/cron.rs:60-64,132-502`; `src/dashboard.rs:1404-1453` | `cron/executions.py:184-228`; `cron/jobs.py:809-855` | Dashboard run history exists; CronTool accepts neither runs nor status. No ticker heartbeat/success state exposed there. Historical claim that only list/get/add/delete exist is false; remaining observability feature is specifically this interface. |
| C18 | P0 #14 happy finite completion / persistent claims | PRESENT | `src/cron/scheduler.rs:762-795,863-969`; `src/cron/store.rs:439-443` | `cron/jobs.py:1515-1570` | Durable claim exclusion and success-count disable exist; sync preserves completed counts and next-run advances. C03/C16 are remaining edges. |
| C19 | P0 #13 scheduler helper suppression; P1 wake parser | PRESENT | `src/cron/scheduler.rs:178-249,1106-1140` | `cron/scheduler.py:298-347,2312-2335` | Token parsing and scheduler suppression work when executor actually returns output. C02/C05 describe missing live integration, not absent helpers. |
| C20 | P2 one-shot 120s grace | PRESENT | `src/cron/scheduler.rs:1359-1384`; `src/tools/cron.rs:408-427` | `cron/jobs.py:656-684` | Creation accepts timestamps through 120s past and rejects older ones. Hermes also does not categorically discard already-persisted old due one-shots, so no invented startup-expiry defect. |
| C21 | P2 workspace AGENTS.md; ACK command bounds | PRESENT | `src/main.rs:526-529,998-1022`; `src/cron/ack.rs:15-35` | `cron/scheduler.py:3362-3365`; ACK is local extension | Hermes-mode workspace instructions injected, truncated to 8K. ACK shell child is timeout-bounded. Actual workdir routing and ACK conditions remain C13/C11. |
| C22 | P2 wrapping, session mirroring policy; native agent/runtime | INTENTIONAL | `src/cron/scheduler.rs:1204-1227`; `src/main.rs:601-603`; `src/discord/adapter.rs:1910-1912` | `cron/scheduler.py:1480-1519,624-654` | Final-only Discord and PNG tables; OMO rather than Hermes intelligence; permanent per-bot conversation semantics take precedence. Default-on local mirroring vs Hermes opt-in is not itself a defect. |
| C23 | cron provider plugins / non-Discord delivery | NOT-APPLICABLE | `src/cron/store.rs:260-263`; `src/cron/scheduler.rs:1340-1342` | `cron/scheduler_provider.py:1-19,122-160`; `gateway/delivery.py:449-548` | External NAS/scale-to-zero trigger and non-Discord topic/voice transports excluded. |
| C24 | suggestions/blueprints/classifier catalogs | NOT-APPLICABLE | `src/tools/cron.rs:51-54` native explicit scheduling contract | `cron/blueprint_catalog.py:1-21,661-713`; `cron/suggestions.py:223-244`; `cron/suggestion_catalog.py:124-154`; `cron/scripts/classify_items.py:147-190` | Proposal/catalog intelligence is outside assigned gateway runtime parity, not a confirmed broken schedule. |

P1 outbound MEDIA also remains a cron-applicable defect: `extract_media_directives` exists at `src/discord/adapter.rs:39`, but only tests call it; both live SendMessage and Stream paths above forward directives literally. Owned as **C11 media subcase**, coordinate with outbound audit rather than duplicate implementation. Hermes extracts at `cron/scheduler.py:1506-1509`.

P1 restart recovery/circuit breaker are partially present (`src/main.rs:1303-1320` interactive resume guard); cron crash replay is C03, not fixed by that guard. P2 drain marker/readiness are lifecycle-owner items, not silently dropped: cron provider's `can_dispatch` at `cron/scheduler_provider.py:177-201` has no analogue in `src/cron/scheduler.rs:494-519`; coordinate lifecycle audit. **One-daemon invariant conflict:** current `src/main.rs:1322-1334` and `src/agent/omo_config.rs:223-239` deliberately start a second cron daemon (default port 19743). This contradicts the supplied one-multiplexed-daemon requirement, not a missing Hermes feature; hand to backend owner, protected files untouched.

## Ownership boundary and integrated-path conclusion

**Migration resync deletion after cutover belongs exclusively to config audit C01.** It is not a cron FIX, implementation task, or additional count here. Cron C01 is the unrelated outbound obligation finding (qualify IDs by audit lane). C09 covers truthful mutation responses while an imported job is read-only; it does not own changing migration authority or deleting/resynchronizing cutover records. C16 covers malformed-record isolation and explicit schedule rearming, not cutover deletion.

The production comparison is backend + executor + scheduler, not helper-only: main wires AgentCronExecutor to OmoBackend and DiscordEgress (`src/main.rs:1329-1347`). For agent jobs, OmoBackend emits final output and conditionally ACKs dispatch success (`src/agent/omo_backend.rs:658-677`), then AgentCronExecutor returns None (`src/main.rs:600-604,703-705`). Consequently scheduler silence/fanout never sees that final result. For script-only jobs, executor returns text and scheduler filters/delivers it, but Hermes no_agent returns before wakeAgent checking (`src/main.rs:492-499`). Scheduler ACK occurs inside per-target delivery and also on error-notification delivery (`src/cron/scheduler.rs:1143-1152,1177-1201`). Thus helper tests passing cannot establish silent-agent suppression, false-gate script silence, complete fanout, or producer checkpoint safety. C02/C05/C11 regressions explicitly exercise these production seams.

## Implementation ownership and executable regression contracts

These are **proposed** tests, commands, and local QA, not tests run or files added. RED/GREEN below are binary observables. Use temporary SQLite/profile roots and local wire fakes; no live Discord or production scripts. Await subscribed final/commit events or execution handles with bounded timeout, never sleeps/polling. REST QA means local dashboard router request through its actual handler with a test DB. WS QA means local OMO-protocol server plus real executor/scheduler. Egress QA means local HTTP capture/failure server at the platform boundary.

File-disjoint implementation packages (rows within a package must be one owner's coordinated change, not parallel overlapping patches):

* **A backend contract**: C02, C05, C06, C13; `src/main.rs`, `src/agent/{backend,omo_backend,omo_protocol}.rs`; new `tests/test_cron_executor_parity.rs`. Protected backend edits require lead coordination, never overwrite user work.
* **B scheduler contract**: C03, C04, C11 scheduling/ACK sequencing, C15; `src/cron/{scheduler,ack}.rs`, new `tests/test_cron_schedule_parity.rs` and `tests/test_cron_delivery_parity.rs`.
* **C ingress/store security**: C07, C08, C10, C16; `src/cron/{store,guard}.rs`, `src/security/scan.rs`, `src/dashboard.rs`; new `tests/test_cron_boundary_parity.rs`. Runtime checks in package A call this validated boundary contract.
* **D tool surface**: C09, C17; `src/tools/cron.rs`, `src/dashboard_runtime.rs`; new `tests/test_cron_tool_parity.rs`. Package A main wiring binds scheduler handle.
* **E outbound/recovery identity**: C01, C12, C14 and C11 media/receipt subcases; `src/ledger/service.rs`, `src/discord/adapter.rs`, `src/mirror.rs`, `src/models/session.rs`, new migration and `tests/test_cron_egress_parity.rs`. A/B call shared receipt/obligation API; main recovery coordination belongs A. No second owner edits A files.

| ID | Minimal change; proposed regression + literal command | Payload and RED -> GREEN; faithful QA |
|---|---|---|
| C01 | Record stable run/turn+destination obligation before final send; retain failures; do not report failed dispatch as success. `cargo test --test test_cron_egress_parity cron_final_obligation_recovers` | Final `report-17`, interrupt at pre-send event, reconstruct sender over same DB: RED zero recoverable rows -> GREEN one recoverable row and one confirmed recovered delivery. Exercise both WS agent and script scheduler output. Preserve bot identity in recovery; add owner start-stamp and retention bounds. Separate fixture with recycled live PID/old start stamp must recover, and expired terminal rows must prune. |
| C02 | Return explicit cron execution result without direct backend emission; scheduler owns filtering/fanout/mirror. `cargo test --test test_cron_executor_parity agent_result_uses_scheduler_delivery` | Hermes deliver `discord:42,discord:43`, WS final `[SILENT]`, then ordinary report in separate case: RED sentinel send / one report target -> GREEN zero sentinel sends / both report targets. Native/local variants must make zero Discord calls. |
| C03 | Consume finite budget and provisionally advance schedule atomically before side effects; record ambiguous interruption distinctly, no automatic replay of same occurrence. `cargo test --test test_cron_schedule_parity finite_budget_survives_failure_and_restart` | `interval:1m`, repeat `{times:2,completed:0}`, executor error twice; RED enabled/count=0 -> GREEN exhausted/count=2. One-shot repeat=1 interrupted after side-effect signal then expired-owner recovery: RED second execution -> GREEN no second execution. Trigger exhausted job likewise rejected. |
| C04 | Persist output by exact profile/job/run independently of delivery, query it rather than session suffix/global fallback. `cargo test --test test_cron_schedule_parity predecessor_is_profile_scoped` | A/B profiles both job `abc123`, outputs `A`/`B`, downstream A `context_from:["abc123"]`: RED missing/B result -> GREEN exact A; script-local predecessor also retrievable. `%` job reference must never match unrelated output. QA run producer then consumer through scheduler. |
| C05 | Gate successful script stdout before no_agent return; distinguish silent vs fallback; reject no_agent without script. `cargo test --test test_cron_executor_parity script_gate_is_silent_in_both_modes` | `no_agent:true`, stdout `{"wakeAgent":false}`, content=`fallback`: RED outgoing JSON/fallback -> GREEN no backend/send/ACK. Empty stdout with nonempty prompt: RED backend run -> GREEN no backend run. Use harmless printf script fixture. |
| C06 | Permit valid skill-only task and normalize bundle slash; validate effective task after resolution. `cargo test --test test_cron_executor_parity skill_only_bundle_runs` | prompt=`""`, skills=`["/bundle"]`, manifest skills `["member"]`, real SKILL.md: RED executor error -> GREEN one backend turn with resolved member. Missing optional skill plus normal task continues. |
| C07 | Central validate every mutation; scan resolved script bytes again before spawn; check ACK; narrow target-specific lifecycle patterns. `cargo test --test test_cron_boundary_parity lifecycle_guard_covers_rest_and_script_body` | REST payload script=`launchctl kickstart gui/501/omon-gateway` and imported `restart.sh` containing same: RED accepted/spawnable -> GREEN rejected before spawn. `launchctl kickstart gui/501/com.example.worker` must be accepted. Never execute lifecycle fixture. |
| C08 | Strict raw directive scan plus loose runtime assembled-data scan at shared boundary. `cargo test --test test_cron_boundary_parity imported_skill_injection_is_blocked` | Imported prompt=`summarize`, skill contains `ignore all previous instructions`: RED backend receives it -> GREEN blocked with zero turn/start. Legitimate data quoting a command must remain accepted. QA imported job through sync + executor. |
| C09 | Bind scheduler, enforce imported read-only, preserve recognized arguments, recompute schedule only when schedule changes. `cargo test --test test_cron_tool_parity mutations_preserve_contract` | Add `enabled:false,repeat:{times:2},context_from:["abc"]`; RED enabled/dropped fields -> GREEN preserved. Pause then trigger: RED no execution -> GREEN one manual run/no schedule consumption. Imported delete must error, not say deleted then resurrect. Name-only update preserves timestamp. |
| C10 | Shared normalized target parser with explicit channel/thread and configured Discord home expansion; isolate malformed record. `cargo test --test test_cron_boundary_parity target_forms_resolve` | Hermes deliver `["Discord:42:43","all"]`, configured home=44: RED document error/invalid ID/no home -> GREEN destinations `(42,43),(44,None)`; QA import then script dispatch to HTTP capture. Implement with C store boundary package. |
| C11 | Aggregate per-target receipts, attempt all targets; ACK once only for successful intended output, never error/silence/skips; wire media extraction/upload and propagate attachment failure. `cargo test --test test_cron_delivery_parity ack_requires_complete_output_delivery` | Deliver 42,43; 42 succeeds,43 fails; ACK=`printf ack` captured safely: RED ACK before failure -> GREEN ACK count 0. All success -> exactly 1. Executor error alert -> 0. Media=`MEDIA:<temp>/a.png`: RED literal/no upload -> GREEN attachment upload; failed upload => no ACK. |
| C12 | Persist bot+channel registry, classify Discord codes not all 404s, return non-delivered receipt for skip, cover Stream, provide recovery probe/clear. `cargo test --test test_cron_egress_parity dead_targets_are_scoped_and_recoverable` | Bot A missing-message 404 on channel42, bot B send same channel: RED blackholed B -> GREEN B sends. Unknown-channel 10003 persists across egress recreation; subsequent send skipped without ACK. Local egress HTTP QA only. |
| C13 | Carry supported per-job runtime options to OMO session/protocol; explicitly reject unsupported provider/base_url/tool restrictions. `cargo test --test test_cron_executor_parity overrides_are_honored_or_rejected` | model=`special`, provider=`alternate`, base_url=`http://127.0.0.1:9999`, workdir under authorized temp root, enabled_toolsets=[]: RED ordinary turn silently ignores options -> GREEN actual parsed routing/cwd matches or clear pre-run unsupported error. Inspect WS frames, not prose. |
| C14 | Persist exact bot/channel/thread identity; destination-specific mirror lookup, never arbitrary latest session; use independent cron execution identity. `cargo test --test test_cron_egress_parity reminder_retains_bot_and_thread` | Create from bot B channel42/thread43 with bot A default; RED A/42 or unrelated mirror -> GREEN B/43 and only matching transcript; repeat after recovery. Second fanout destination must not be mirrored into source twice. |
| C15 | Persist/import timezone and evaluate cron wall-clock schedule there, convert next instant to UTC only for storage. `cargo test --test test_cron_schedule_parity imported_cron_retains_timezone` | profile Asia/Seoul, `0 9 * * *`, completion `2026-09-05T00:01:00Z`: RED next `09:00Z` -> GREEN `2026-09-06T00:00:00Z`. QA import/run/completion/reload using injected fixed clock. |
| C16 | Isolate invalid records/profiles, preserve healthy boot; distinguish operator rearm from unchanged terminal import; expose current run/delivery state. `cargo test --test test_cron_boundary_parity sync_isolates_bad_jobs_and_rearms_changed_once` | One valid job plus malformed schedule; RED sync/boot error -> GREEN valid job available + invalid diagnostic. Completed once changed to future run_at: RED disabled -> GREEN enabled future next time. Increase finite limit likewise; fetched status reflects latest execution. |
| C17 | Add bounded runs/status tool operations against existing run store and ticker health. `cargo test --test test_cron_tool_parity runs_and_status_are_exposed` | `{"action":"runs","id":"j"}` with one seeded attempt; RED unknown action -> GREEN one structured attempt; status reports running/paused and ticker health. QA actual Tool.execute surface, no prose assertion. |

## Existing coverage and limitations

Existing coverage located (helper bodies read where relevant; none run): `test_repeat_times_disables_job_after_limit_reached`, `synchronization_does_not_rearm_completed_repeat_times`, `synchronization_preserves_scheduler_advanced_next_run`, `test_execute_job_suppresses_delivery_for_silent_and_empty_output`, `test_parse_wake_gate`, `test_oneshot_grace_window`, `test_load_cron_skills_missing_skill_resilience`, `test_build_cron_llm_config_overrides`, `test_delivery_fan_out_parsing`, `test_mirror_cron_delivery_to_session`, `test_obligation_lifecycle_transitions`, `test_sweep_recoverable_selection_and_pruning`, `test_recover_pending_delivery_obligations_redispatches_dead_process_rows`, `test_deliver_runs_ack_command_after_delivery`. These cover intended helper contracts, not end-to-end production integration; no passing-test claim is made. Voice-cron integration covers healthy claims, failed one-shot disable, backoff, live/dead lease ownership; not output recovery/finite failures.

Test-quality finding (existing, not edited): `tests/test_voice_cron.rs:183-267` uses a fixed 200-yield polling budget and wall-clock upper bounds; replace with committed completion signal. `src/cron/scheduler.rs:1793-1797` pins formatted context prose; replace with machine-data/provenance assertions rather than preserving wording. Several lease tests await unbounded recv; bound event awaits. ACK sleep tests at `src/cron/ack.rs:88-94` test timeout itself and are not forbidden fixed-sleep synchronization.

High-risk merge recommendation: fix C01/C02/C03/C07/C11 and bot identity C14 before claiming P0 parity. Do not accept helper existence, a dashboard notification, or dispatcher Ok on a filtered/skipped send as platform ACK. Durable at-least-once recovery must label ambiguous retries; finite side-effect jobs must not silently rerun after an unknown crash.
