# Config / migration parity audit - 2026-09-05

Status: COMPLETE (source audit; no implementation). Counts: **14 FIX, 6 PRESENT, 3 INTENTIONAL, 3 NOT-APPLICABLE = 26 inventory rows**. FIX rows group defects sharing a repair boundary; they are not counts of individual missing settings. Highest risk: C01, C02/C03, C05-C07. Feature omissions are distinguished from confirmed broken behavior below.

Reference: local Hermes HEAD `146f4ed07d1c10031c86a5e7f2ac78d11b253dd9`, confirmed with git; local modifications were read as the current reference, not altered. Rust locations relative to repository; `H:` locations relative to `/Users/indo/.hermes/hermes-agent`. Read all `src/migrate/**`, entry, README, assigned tests, main/runtime configuration and their immediate consumers, Hermes gateway config/display/profile modules and relevant CLI normalization/env/profile/migration paths. LSP `lsp_symbols(src/main.rs)` failed: daemon unreachable. Used ast-grep Rust struct queries plus direct reads and caller searches instead. Programming/Rust skill read. No tests, builds, migrations, daemon operations or Discord requests executed. RED/GREEN below are **proposed tests and source-derived predictions, not executed results**.

## Inventory and old-backlog dispositions

| ID | Status / old mapping | Rust evidence; Hermes evidence | Mechanism / disposition |
|---|---|---|---|
| C01 | FIX P0; new migration defect | `src/migrate/mod.rs:174`, `cron_cutover.rs:204-218`; `src/main.rs:1293-1295`; `src/cron/store.rs:445-454`; H:`cron/jobs.py:858-934` | Full cutover empties source stores but leaves `_omon_hermes_source` on imported rows. First normal startup sync sees no live IDs and DELETEs every migrated job; scheduler repeats sync at `scheduler.rs:510`. A successful migration loses its cron schedules on startup. |
| C02 | FIX P0/P1; P1 channel allow/ignore, role/pairing, approval config; P2 mention/bot/topic/backfill knobs (import fidelity) | `config_import.rs:7-50,257-345`; `main.rs:105-125,176-250,1357-1373`; H:`gateway/config.py:1187-1216,1373-1558,1698-1720`; H:`plugins/platforms/discord/adapter.py:9294-9408` | Import schema admits only model/approval-mode/two Discord token fields; output admits four scalar env keys. Drops allowed/ignored channels, roles, YAML `allow_from`, approval deny globs, reaction/mention/backfill/bot settings and channel overrides. Importing allowed user 42 plus ignored channel 99 drops exclusion so authorized user 42 can now invoke bot in 99. YAML-only token under `platforms.discord.token` is lost, as is root `discord.token`; `enabled:false` is ignored and token imported anyway. Existing gateway JSON-only configs fail because config.yaml is required (`:143`). Model aliases `model.name/model.model/api_base` are ignored, scalar model fails; H CLI normalizes these at `config.py:6875-6957,7413`. Unsupported applicable policies are silently discarded rather than reported. |
| C03 | FIX P0/P1; P1 profile routing, session-context isolation | `config_import.rs:308-338`; `src/multiplexer/profile_routing.rs:52-81`; `main.rs:1251,1357-1373`; H:`gateway/profile_routing.py:51-64`; H:`gateway/run.py:18860-18928`; H:`hermes_cli/profiles.py:949-987` | All profile tokens share one global ACL and first-profile scalar fallback. Work profile user 2 is denied if root allows only user 1; root user 1 gets work-bot access despite profile restriction. Profile name/config/prompt/model/workspace/memory associations are not emitted. Hermes route `profile: work` is not represented by Rust inline routes (unknown field ignored). Numeric hierarchy exists, actual Hermes profile selection does not. |
| C04 | FIX P1; new dotenv fidelity | `config_import.rs:211-252,378-420`; `src/entry.rs:63`; H:`hermes_cli/env_loader.py:162-166,288-335` | Custom parser is not dotenv: `export DISCORD_BOT_TOKEN=x` becomes wrong key; quoted value followed by inline comment is retained with quote/comment; interpolation/escapes differ. Writer strips protective quotes and emits raw `$`/`#`/space characters; next dotenvy load may change secrets/model values. E.g. YAML api_key `a # b` writes unquoted `OPENAI_API_KEY=a # b` and reload truncates. |
| C05 | FIX P0/P1; new secure/atomic persistence | `config_import.rs:106-119,458-463`; `src/migrate/sys.rs:80-81`; H:`hermes_cli/profiles.py:1115-1124`; H:`hermes_cli/config.py:7125-7146` | `std::fs::write` creates secret-bearing target AND backup using umask (0644 under 022); copying a 0600 original does not copy permissions. Target rewrite truncates in place, so interrupted write corrupts active config. Second migration within the same second overwrites the only `.bak-YYYY...SSZ` backup. Cron backups use the same unprotected write primitive. |
| C06 | FIX P0; new lifecycle safety | `gateway_down.rs:24-25,48-87,130-139,163-165`; `sys.rs:153-166`; H:`gateway/status.py:2016-2044`; H:`hermes_cli/gateway.py:4318-4345` | Any positive PID in stale JSON is signaled without start-time/process identity/held-lock validation: PID reuse kills unrelated processes. TERM/KILL precede launchd bootout, enabling KeepAlive respawn; immediate post-KILL existence check can spuriously fail before exit. Existing `.plist.disabled` makes discovery skip a live matching `.plist`, leaving service running. Unreadable/malformed lock is silently skipped. |
| C07 | FIX P0/P1; new cutover consistency | `mod.rs:153-180`; `cron_cutover.rs:62-75,116-121,186-192`; H:`cron/jobs.py:257-351,910-934`; H:`hermes_cli/gateway.py:4318-4345` | Verification proves ID existence only, not content/source/import receipt. Hermes can update same ID between sync and cutover's first read: updated bytes become `original`, pass later stale-byte check, and are emptied while old payload remains in SQLite. Hermes still runs until after emptying, so in-flight work can rewrite stores. Per-store mutation allows partial cutover if a later store fails; error skips gateway-down. |
| C08 | FIX P1; new dry-run/apply disagreement | `mod.rs:89,194-214,265-325`; `store.rs:348-389,402-414`; H:`cron/jobs.py:512-609` | Dry-run parses IDs only and sets `would_empty=true` regardless of valid schedule/lifecycle guard or `OMON_HERMES_PROFILES`. Real import honors profile restriction and skips lifecycle jobs. A missing schedule / forbidden restart job previews as importable, then apply fails or refuses cutover after writing config/DB. |
| C09 | FIX P1; new cron import schedule fidelity | `store.rs:188-213,426-431`; `scheduler.rs:1377-1410`; H:`cron/jobs.py:720-777`; H:`hermes_time.py:37-75,122-133` | `next_run(...).ok()` swallows malformed/unsupported schedules into enabled rows with NULL next-run; cutover accepts them by ID. A stored next_run bypasses schedule validation entirely. Cron recurrence always uses UTC whereas Hermes uses configured/local timezone; a 09:00 Seoul job may retain first instant then move to 18:00 Seoul. Interval recovery ignores last_run_at when next_run_at is missing. |
| C10 | FIX P1/P2; P2 cron fan-out (partial) | `store.rs:216-274`; `main.rs:557`; `scheduler.rs:1170-1190`; H:`cron/scheduler.py:1136-1297`; H:`gateway/config.py:1756-1763` | `discord`/`all`/missing-origin only use origin, never configured Discord home. No-origin `deliver:discord` silently yields no target despite home channel 42. `all` does not expand configured homes. Explicit thread references/name labels are treated as raw channel text. Non-Discord exclusions do not justify losing the Discord member of fan-out. |
| C11 | FIX P1; P1 profile toolsets; P2 per-job provider/base_url | `main.rs:580-604,974-996,1237-1250`; `omo_protocol.rs:23-80`; `omo_backend.rs:284-289`; H:`gateway/run.py:14803-14844`; H:`cron/jobs.py:1017-1070` | Model and initial system prompt reach daemon; enabled_toolsets, per-job provider/base_url do not. `build_cron_llm_config` is a legacy helper with no active executor call. Route toolsets are stored on session but never serialized to thread/start or turn/start; advertised restricted toolset cannot restrict daemon tools. Importing custom provider merely guesses Anthropic from model prefix; no provider-qualified mapping/provisioning exists in active request protocol. This is configuration wiring, not a request to reimplement agent intelligence. |
| C12 | FIX P1; README defaults / model selection | `main.rs:187,1237-1240`; `dashboard_runtime.rs:115`; `omo_config.rs:183-189`; H:`hermes_cli/config.py:6875-6957,7150-7164` | README gpt-4o default is actually required DEFAULT_MODEL; OMON_DEFAULT_MODEL alone still fails gateway startup. With both variables set, gateway overwrites backend's OMON_DEFAULT_MODEL preference with DEFAULT_MODEL; dashboard does not. Same environment selects different default models depending on surface. |
| C13 | FIX P2; P2 runtime footer; README truthfulness | `main.rs:80,241-244,1350-1375`; `dashboard_runtime.rs:279,308`; `discord/adapter.rs:238-291`; `README.md:126-167,224-240`; H:`gateway/display_config.py:182-241` | Footer boolean is parsed/displayed but neither copied into PoiseData nor used by active egress; helper has only test callers. README says unrelated target keys removed, yet merge deliberately preserves them. Binary advertised as `omon-gateway` but Cargo target is `omo-gateway` (`Cargo.toml:18-19`). Dashboard reports default approval `ask` though actual parse defaults Smart. Correct docs, not tested prose. |
| C14 | FIX test quality; profile-routing coverage | `tests/test_profile_routing.rs:130-142,231-243`; H:`gateway/profile_routing.py:151-166` | Both integration tests poll 500 x 10ms after action, despite comments claiming determinism; completion depends on timing. They prove session projection only, not daemon enforcement. Existing tests should be kept, changed to subscribe-before-action exact completion signaling. |
| P01 | PRESENT; migration basics | `config_import.rs:77-103,430-455`; `cron_cutover.rs:175-219`; H:`cron/jobs.py:257-351,910-934` | Masked diffs, pre-parse failure before config write, preservation/backup of old target, lock + stale-byte comparison + atomic cron rename. Existing tests `merges_into_existing_env_and_backs_up_original`, `stale_store_state_is_rejected_before_backup`, `dry_run_projects_every_step_with_zero_writes_or_side_effects`. These do not cover C01/C05/C07. |
| P02 | PRESENT; P1 hierarchy portion of profile routing | `profile_routing.rs:99-163,191-250`; `tests/test_profile_routing.rs:23-81`; H:`gateway/profile_routing.py:65-104,146-166` | Stable thread > channel > guild conjunction, disabled routes, ID aliases, explicit model preservation. C03/C11 remain. |
| P03 | PRESENT (import layer); P0 #14 repeat | `store.rs:104-110,440,731-780`; H:`cron/jobs.py:1550-1573` | Repeat counts imported, max completed preserved and exhausted schedules not rearmed by resync. `synchronization_does_not_rearm_completed_repeat_times`; scheduler has `test_repeat_times_disables_job_after_limit_reached`. Runtime accounting details belong cron lane. |
| P04 | PRESENT (configuration/executor wiring); P1 context_from, missing skill, wakeAgent gate; P2 skills/AGENTS | `main.rs:495-546,708-790,801-903,1000`; H:`cron/scheduler.py` context/skills executor; H:`cron/jobs.py:376-399` | Pre-run gate precedes agent; predecessor output and workspace instructions appended; missing skills with prompt warn/continue; bundle resolver exists. Existing `test_load_cron_skills_missing_skill_resilience`. Cron lane owns payload/empty-result/agent-output correctness, not asserted here as fully green. |
| P05 | PRESENT; P1 restart recovery/breaker; P2 drain/readiness | `main.rs:1303-1320,1389-1400,1414-1443`; H:`gateway/status.py:1538-1594`; H:`hermes_cli/gateway.py:4318-4345` | Configured restart guard and recovery called; readiness logged; drain marker shutdown connected. Shutdown/event correctness is runtime lane. |
| P06 | PRESENT (runtime option wiring); P1 ACL/roles/threads; P2 allow_bots, thread_require_mention, channel topics/prompts/backfill; P2 pagination | `main.rs:176-250,1352-1373`; `discord/throttler.rs:237,277`; H:`plugins/platforms/discord/adapter.py:9294-9408` | Rust environment controls are passed to adapter/router; no blanket MISSING claim remains. Import loss C02 and named-profile C03 remain. Inbound/output lanes own exact semantics. |
| I01 | INTENTIONAL; P0 #11/#12, P1 live edit/status, display verbosity | `omo_backend.rs` final-only path; H:`gateway/display_config.py:34-74,182-241`; H:`gateway/config.py:691-784` | Final-only responses, no tool/reasoning/status chatter, persistent immediate typing and PNG tables supersede Hermes display knobs. Do not restore preview streaming or disable typing for parity. |
| I02 | INTENTIONAL; session reset/pruning/group-per-user configuration | `main.rs:1252-1258`; H:`gateway/config.py:456-510,870-896` | Permanent dedicated bot DM/channel/thread sessions and per-agent workspace/memory win over automatic daily/idle reset, pruning and participant splitting. Runtime violations of invariant remain runtime-lane defects, not missing Hermes features. |
| I03 | INTENTIONAL; concurrency/backend architecture | `main.rs:1237-1250,1322-1336`; H:`gateway/config.py:874-879`; H:`hermes_cli/profiles.py:949-987` | One multiplexed interactive daemon with per-agent state is required. Do not restore one gateway per Hermes profile. Separate cron-daemon wiring is explicitly visible; lead/runtime lane must reconcile with one-daemon invariant, not justify it as parity. 300s interactive cap is intentional (backend clamps at `:393-398`). |
| N01 | NOT-APPLICABLE; P2 cross-platform transcript mirroring, non-Discord configs | `store.rs:248-251`; H:`gateway/config.py:1803-2544` | Other-platform credential/env bridges, HTTP relay/webhook listeners and cross-platform egress excluded. Preserve non-Discord source jobs or flag exclusion during cutover; not an authorization to silently retire unsupported workloads. |
| N02 | NOT-APPLICABLE; systemd/cgroup/watchdog/cloud administration | `gateway_down.rs:74-87`; H:`gateway/config.py:153-190,882-884` | Local macOS launchd scope. Linux service migration is not requested despite broad README runtime platform badge. |
| N03 | NOT-APPLICABLE; agent intelligence/CLI-specific migrations | `omo_config.rs:6-29`; H:`hermes_cli/migrate.py:16-115`; H:`gateway/display_config.py:29-32` | Hermes xAI retirement rewrites, agent inference routing/fallback/compression, skins/CLI output, managed enterprise administration and secret-provider fetching are independent engine/CLI capabilities. Gateway must report unresolved imported credentials/provider bindings rather than pretend they were migrated (C02/C11). |

Additional configuration inventory: Hermes quick_commands/reset_triggers, max_concurrent_sessions, unauthorized-DM pair-vs-ignore, notice_delivery, admin/group command ACLs, mention_patterns/exclusive mentions, no_thread_channels, reply mode/prefix, channel_skill_bindings, configurable allowed mentions, proxy, STT echo/enable and WebSocket liveness are not represented by the import schema. These are **feature omissions**, not individually proven runtime defects in this lane. C02's minimal safe contract is explicit unsupported-key dispositions before destructive cutover, not silent loss. Inbound/output/security lanes own implementing those surfaces. Default auto-thread differs (Hermes true at adapter:7168; Rust false at main:202); document/map effective source value, do not blindly replace user's default. HOME/free-response and approval timeout differences likewise require explicit normalization, with intentional session/display exceptions above.

Old backlog ownership boundary: P0 #13 silence and P1 lifecycle/threat scanner/CronTool operations, outbound MEDIA, cron output delivery and P2 one-shot grace/mirroring are cron/output-lane owned; inspected import retains those fields and current runtime has gate/helpers, not a config-lane claim that all end-to-end semantics pass. P2 provider override and fan-out explicitly remain C11/C10; footer C13; skills/workspace P04; readiness/drain P05; DM pairing and text/voice/backfill/forwarded snapshots/channel prompts/options P06/C02 or inbound lane; multi-tool approval/Tirith and P1 approval scopes/staging/allowlist are security lane (main:1197-1214 configures Tirith on native terminal, not proof of daemon enforcement). No owned P2 item is silently dropped.

## FIX implementation contracts / regression and local QA

All commands are proposals from repo root. Each named test is new unless explicitly called existing above. Use temporary homes/SQLite, synthetic credentials, in-process fake OS for signals, local WebSocket/HTTP recording servers; never live Hermes/Discord. Source locations above were re-read and traced through entry -> migrate / run_gateway and executor -> backend before claims. File scopes below are **ownership slices**, not permission to parallel-edit shared files; serialize C01/C07/C08 (`mod.rs`), C02-C05 (`config_import.rs`), C09/C10 (`store.rs`), C11/C12 (`main.rs`). Protected backend files require lead coordination.

- **C01:** Detach imported rows from live-source deletion using durable cutover ownership, atomically before retirement. Scope `cron_cutover.rs`, `store.rs` source-deletion filter, migration persistence/test. Test `cutover_survives_startup_sync`; `cargo test --test test_migrate cutover_survives_startup_sync`. Payload daily job `{"id":"daily","prompt":"status","schedule":{"kind":"cron","expr":"0 9 * * *"}}`. RED count 1 -> 0 after migrate then production synchronizer; GREEN count remains 1 after two syncs. QA fixture migration then exact startup sync and due-job claim, no Discord.
- **C02:** Read effective supported YAML/legacy/env forms; emit supported controls and report/refuse unresolved security/credential settings before writes. Scope importer schema/map/preflight + importer tests. `cargo test --lib migrate::config_import::tests::imports_effective_discord_policy`. Payload token x, allow user42, ignored channel99, YAML `enabled:false`, aliases `model.name:gpt-x` in separate cases. RED lost keys or disabled bot activated; GREEN preserved mapped policy, disabled token not started, unsupported cases typed refusal with zero writes. QA run `migrate --dry-run` in synthetic HOME then `--no-cutover`; parse generated config with real runtime parser and replay authorized user42/channel99 through filter (denied).
- **C03:** Persist bot-token-to-profile association and per-bot policy/persona/agent mapping; translate named Hermes routes or refuse conflicting profiles until supported. Scope profile manifest/import section, router configuration and bot adapter policy selection, tests. `cargo test --test test_profile_routing imported_profiles_preserve_bot_policy`. Payload root token A/users1, work token B/users2 with route chat20/profile work. RED B accepts root policy; GREEN user1 denied B, user2 accepted, selected agent/profile work. QA fixture import plus two bot-identity inbound events and recorded daemon workspace/prompt selection.
- **C04:** Replace value parsing with dotenv-compatible parsing and safe escaping; keep raw unrelated lines. Scope importer env document/renderer only. `cargo test --lib migrate::config_import::tests::dotenv_round_trip_special_values`. Payload `export DISCORD_BOT_TOKEN='x' # comment`, YAML key `a # b`, literal dollar. RED missing/corrupt values; GREEN real dotenvy reload equality. QA synthetic `--no-cutover`, parse output without exporting global environment.
- **C05:** Exclusive unique backups, 0600 files, same-directory atomic target replacement; preserve original on failure. Scope secure OS write API and importer/cron backup call sites. `cargo test --test test_migrate secret_files_are_private_and_backup_unique`. Payload 0600 target and two imports at identical injected time; injected failure before rename. RED group/other permission bits or overwritten first backup/truncated target; GREEN all zero, both backups retained, original intact. QA temp directory under umask022 with OsEnv only; stat files; no signals.
- **C06:** Validate start-time/identity before any signal; unload matching live service before stopping residual verified PID; await bounded exit; handle existing disabled destination without skipping active service. Scope gateway_down/sys process APIs. `cargo test --lib migrate::gateway_down::tests::stale_pid_never_signaled`. Payload `{pid:4242,start_time:1}` while fake process4242 start2; active plist plus disabled backup. RED signal4242 and/or no bootout; GREEN zero signals to reused PID and bootout active service. QA scripted fake launchd/process lifecycle events, deterministic exit signal, no real launchctl.
- **C07:** Quiesce verified source service before final snapshot, lock/revalidate whole import receipt and source set, compare canonical payload rather than ID; retain source on mismatch. Scope migration orchestration/cutover receipt. `cargo test --test test_migrate changed_payload_blocks_cutover`. Payload DB daily/prompt old then source same ID/prompt new before cutover read. RED emptied source with old DB; GREEN refuses without source mutation or imports new under lock. QA same real temp-store/SQLite sequence with fake OS; assert operation ordering and every store's bytes on injected second-store failure.
- **C08:** Share store selection and pure full-job validation between projection/apply; include rejected jobs/reasons. Scope mod projection + validation seam in store. `cargo test --test test_migrate dry_run_reports_nonimportable_jobs`. Payload id bad with missing schedule; selected profiles work while default has job; forbidden restart prompt. RED would_empty true/importable; GREEN explicit rejected/nonselected with would_empty false. QA fixture CLI `migrate --dry-run` matches import-only validation, zero writes.
- **C09:** Validate full expression before insertion and propagate errors; persist configured source timezone and recurrence anchor. Scope store expression/next-run import and scheduler timezone recurrence. `cargo test --lib cron::store::tests::imports_timezone_and_rejects_invalid_schedule`. Payload expr `garbage`; separate 09:00 Asia/Seoul after `2026-09-05T00:00:00Z`. RED NULL enabled row / next09:00Z; GREEN rejection before cutover / next `2026-09-06T00:00:00Z`. QA synthetic import, explicit-clock next-run evaluation twice including DST zone case, no timed waiting.
- **C10:** Resolve source-profile Discord home/thread and canonical target refs for origin/discord/all; keep explicit fan-out dedup. Scope store destination resolver plus call sites receiving config. `cargo test --lib cron::store::tests::discord_delivery_uses_profile_home`. Payload home42/thread43, no origin, deliver discord then all. RED zero targets; GREEN exactly (42,43). QA script-only fixture job plus recording dispatcher, assert one correct destination without external send.
- **C11:** Enforce routed tool allowlist at daemon configuration boundary; map provider-qualified models/config only through supported OMO mechanisms or reject unsupported overrides rather than claim applied. Scope gateway request-building/config bridge; coordinate protected backend with lead. `cargo test --test test_profile_routing routed_tools_reach_daemon`; `cargo test --test test_migrate provider_override_is_not_silently_dropped`. Payload route tools `[web]`, cron model x/provider custom/base_url local stub. RED unrestricted daemon request/default provider; GREEN daemon tool restriction excludes terminal and override hits stub or explicit preflight rejection. QA local protocol server records actual thread/start/turn/start, not SessionState-only mock. No independent Hermes agent reimplementation.
- **C12:** One shared default model precedence/default contract for gateway/dashboard; honor OMON_DEFAULT_MODEL, then DEFAULT_MODEL, then documented fallback (or explicitly document required config). Scope main/runtime default selection + tests; no backend rewrite necessary. `cargo test --bin omo-gateway default_model_precedence_matches_dashboard`. Payload OMON_DEFAULT_MODEL=A, DEFAULT_MODEL=B then both absent. RED gateway B/dashboard A or startup missing-key failure; GREEN same configured A and agreed absent behavior. QA both entrypoint config construction against same isolated environment; recorded daemon model fields.
- **C13:** Connect final footer opt-in to active final-message path or remove unsupported advertised knob; correct merge/binary/default documentation and dashboard effective policy display. Scope final egress config, README, runtime view. `cargo test --test test_profile_routing runtime_footer_flag_reaches_final_egress`. Payload footer true/model x/cwd temp, one final response. RED identical output with flag on/off; GREEN metadata present only on final when enabled. QA record local dispatcher final output; dashboard JSON policy equals actual parsed enum. No prose-pinning tests for README.
- **C14:** Replace polling captures with channel/Notify completion registered before route, bounded timeout. Scope `tests/test_profile_routing.rs` only. `cargo test --test test_profile_routing`. Payload existing two routed events and explicit-model case. RED current tests require polling sleeps; GREEN no sleeps, both exact completion signals observed and same state assertions. QA run once against real multiplexer/in-memory SQLite; never use retries to claim green.

## Recommendation

Do not use full cutover until C01 and C05-C07 are fixed and fixture-tested. Import-only remains non-destructive to Hermes but is not a faithful migration until C02-C04 are resolved. Existing migration test stops before normal gateway startup, explaining why C01 escapes it. The `RuntimeConfigProjection` test in `config_import.rs:469-520` reimplements a subset of parsing instead of executing Config::from_env; it cannot validate actual defaults, ACLs, backend provider wiring, or dotenv reload.
